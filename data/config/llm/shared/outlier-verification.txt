These spots were flagged by our quality system for suspicious data. For each one, search the web and determine whether the listed times and schedule are ACCURATE.

{FLAGGED_ITEMS}

For each item, verify:
1. Is the start time correct? Search for the venue's actual {ACTIVITY_TYPE} schedule.
2. Is the end time correct?
3. Are the days correct?
4. Do the activity times fall within the venue's actual operating hours?

Return ONLY a JSON array. Each element:
{
  "index": <0-based position>,
  "verdict": "correct" | "incorrect" | "unknown",
  "corrected_time_start": "HH:MM or null if correct/unknown",
  "corrected_time_end": "HH:MM or null if correct/unknown",
  "corrected_days": "0,1,2,3,4,5,6 format or null if correct/unknown",
  "reasoning": "one sentence citing source"
}
