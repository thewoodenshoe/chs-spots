import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // Note: allowedDevOrigins will be needed in a future Next.js version
  // to suppress cross-origin warnings when accessing from network IPs.
  // For now, the warning is informational and can be safely ignored.
};

export default nextConfig;
